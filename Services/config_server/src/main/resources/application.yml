spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: C:\Project Streamer Config
          search-paths: '{application}'
server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: "*"

