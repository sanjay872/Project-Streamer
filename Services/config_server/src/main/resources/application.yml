spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: C:\Project Streamer\Services\config
          search-paths: '{application}'
server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: "*"

